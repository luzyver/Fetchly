server {
    listen 80;
    listen [::]:80;
    server_name getfetchly.online;

    return 301 https://getfetchly.online$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name getfetchly.online;

    ssl_certificate /etc/letsencrypt/live/getfetchly.online/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/getfetchly.online/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:5050;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_buffering off;
        proxy_read_timeout 300s;
    }
}
